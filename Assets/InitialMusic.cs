using UnityEngine;

public class InitialMusic : MonoBehaviour
{
    private static InitialMusic instance;

    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
